<%= form_for @micropost do |f| %>
  <%= render 'shared/error_messages', :object => f.object %>
  <div class ="field">
    <!--%= label_tag :micropost_content_count, "Characters remaining:" %-->
    <!--%= text_field_tag :micropost_content_count, 
                       140, :class => 'text-count', :disabled => true %-->
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit "Post" %>
  </div>
<% end %>
